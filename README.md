# MEGATID-firmware
MEGATIDE is an open-source ATmega328P dual modulation and clock processor with eight modes, six analog controls, four PWM outputs, tap/external sync, triggers, SPI configuration, EEPROM presets, and separate AVR-GCC and Arduino firmware implementations.
